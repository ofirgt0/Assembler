#ifndef STRING_CONVERTER_H
#define STRING_CONVERTER_H

/**
 * Convert a string to binary representation.
 * @param input The input string.
 */
void stringToBinary(const char *input);

/**
 * Convert a string to base64 representation.
 * @param input The input string.
 */
void stringToBase64(const char *input);

#endif /*STRING_CONVERTER_H*/
